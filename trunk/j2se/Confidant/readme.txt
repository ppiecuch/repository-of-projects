项目名：Confidant
描述：实现按键记录及按键脚本的执行
功能：
1、支持鼠标、键盘按键记录
2、支持脚本生成及脚本导入
3、支持多计时器并行计时
4、不记录本窗口的鼠键操作
5、只记录鼠标的左键事件，并且只记录鼠标click事件，不记录拖动事件
6、只记录键盘按键的释放事件
7、保存脚本的时间以秒为单位，内存数据则以毫秒为单位(在读入写出时进行转换)
8、计时器支持停止与运行

待完成：
1、TaskThread的功能测试：是否会在空时wait
2、可视化模块的设计编码
3、本窗口不记录的技术实现
4、Recorder中，第一个操作的delay是0呢还是按公式进行计算？
5、异常：使用记录器记录的操作在Calculagraph中能全部正确执行，但使用导入操作方式时，最后一个操作不被执行
6、存在某些操作是连体操作，如何解决？

进度：需求分析->系统设计->搭建架构

beta01:
1、支持鼠标、键盘按键记录
2、支持脚本生成及脚本导入
3、只支持单计时器
4、支持记录模式及模拟模式
5、非窗口化