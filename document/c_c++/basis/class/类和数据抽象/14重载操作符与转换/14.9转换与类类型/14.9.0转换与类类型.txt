在第 12.4.4 节介绍过，可用一个实参调用的非 explicit 构造函数定义一个隐式转换。当提供了实
参类型的对象而需要一个类类型的对象时，编译器将使用该转换。这种构造函数定义了到类类型的转换。

除了定义到类类型的转换之外，我们还可以定义从类类型的转换。即，我们可以定义转换操作符，给
定类类型的对象，该操作符将产生其他类型的对象。像其他转换一样，编译器将自动应用这个转换。
在介绍如何定义这种转换之前，将说明它们为什么可能有用。

